{
  "name": "real-time-web-stt-translator",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace client",
    "setup:local-runtime": "powershell -ExecutionPolicy Bypass -File server/scripts/setup-local-runtime.ps1",
    "setup:local-runtime:cuda": "powershell -ExecutionPolicy Bypass -File server/scripts/setup-local-runtime.ps1 -LlamaWheel cu124",
    "local-runtime": "powershell -ExecutionPolicy Bypass -File server/scripts/start-local-runtime.ps1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
